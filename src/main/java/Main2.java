import controller.PhoneService;
import entities.Contact;
import entities.ContactType;
import entities.Person;

import java.util.List;

public class Main2 {
/*
    public static void main(String[] args){
        checkCreate();
    }

    public static void checkCreate(){
        Person person = new Person("vadim", "grigorev", "anatolyevich", "jun");
        ContactType contactType = new ContactType("work");
        Contact contact = new Contact(person,contactType,"8931244445");
        PhoneService service = new PhoneService();
        List<Contact> contactList = service.createContact(contact);
        contactList.forEach(System.out::println);

    }

    public static void checkDelete(){
        PhoneService service = new PhoneService();
        List<Contact> contactList = service.deleteContact(1);
        contactList.forEach(System.out::println);
    }

    public static void checkModify(){
        Person person = new Person("vadimus", "quo", "a", "jjj");
        ContactType contactType = new ContactType("ork");
        Contact contact = new Contact(person,contactType,"33333333");
        contact.setId(2);
        PhoneService service = new PhoneService();
        List<Contact> contactList = service.modifyContact(contact);
        contactList.forEach(System.out::println);
    }

    public static void getContacts(){
        PhoneService service = new PhoneService();
        service.getContacts().forEach(System.out::println);
    }
    */
}
